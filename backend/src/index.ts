/**
 * Backend entrypoint placeholder.
 *
 * The NestJS + Apollo Server application (GraphQL schema, upload/list/status/metadata resolvers,
 * health checks, file-serving route, and the single-writer metadata write path) is implemented in
 * Tasks 3–6. This stub only confirms the shared package is wired in and the workspace builds.
 */
import { ProcessingStatus } from '@video-platform/shared';

export const BACKEND_PLACEHOLDER = `backend scaffold (initial status: ${ProcessingStatus.PENDING})`;
