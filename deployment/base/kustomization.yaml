apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# All base resources are deployed into the video-platform namespace.
namespace: video-platform

resources:
  - namespace.yaml
  - nfs-provisioner.yaml
  - uploads-storage.yaml
