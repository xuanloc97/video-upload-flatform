{
  "name": "@video-platform/processing",
  "version": "0.1.0",
  "private": true,
  "description": "FFmpeg processing worker (renditions, thumbnail, recovery/retry). Scaffolded in Task 8.",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@video-platform/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
