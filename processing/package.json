{
  "name": "@video-platform/processing",
  "version": "0.1.0",
  "private": true,
  "description": "FFmpeg processing worker (renditions, thumbnail, recovery/retry).",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@video-platform/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.0",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
